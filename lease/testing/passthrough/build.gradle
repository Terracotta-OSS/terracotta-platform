plugins {
  id 'org.terracotta.build.convention.java-library'
  id 'org.terracotta.build.convention.deploy'
  id 'biz.aQute.bnd.builder'
  id 'org.terracotta.build.convention.voltron-client'
}

dependencies {
  api project(':lease:client')
  implementation "org.terracotta:passthrough-server:$terracottaRuntimeVersion"
}

deploy {
  groupId = 'org.terracotta'
  artifactId = 'terracotta-lease-testing-passthrough'
  name = 'Lease Passthrough Support'
}
