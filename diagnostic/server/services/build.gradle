plugins {
  id 'org.terracotta.build.convention.java-library'
  id 'org.terracotta.build.convention.deploy'
  id 'org.terracotta.build.convention.voltron-server'
}

dependencies {
  service project(':common:json')
  service project(':diagnostic:server:api')
  implementation project(':diagnostic:common')
  testImplementation project(':common:test-utilities')
}

deploy {
  groupId = 'org.terracotta'
  artifactId = 'terracotta-diagnostic-server-services'
  name = 'Diagnostic Server Plugin'
}
